In Visual Studio Code, go to File > Open Workspace From File..., then select the file "htmlproject.code-workspace"
Then, in the corner when prompted to install extensions, select "Show Recommendations" and install all 3 Python 
extensions, then install one of the browser debug extensions, and edit the file ".vscode/launch.json" to start the
correct browser debug extension.
